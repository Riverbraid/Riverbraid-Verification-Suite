name: Riverbraid Verification Suite
on: [push]
permissions:
  contents: read
jobs:
  verify-integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run bounded audit target
        run: node audit_final.js
